services:
  - type: web
    name: your-app
    env: python
    runtime:
      version: 3.10
    build:
      command: pip install -r requirements.txt
      render: true
    start:
      command: python app.py
    envVars:
      - key: SECRET_KEY
        value: secret_key